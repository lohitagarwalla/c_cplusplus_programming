#include<stdio.h>
#include<conio.h>
int main()
{
    int n;
    printf("enter a integer to convert it to a string:\n");
    scanf("%d",n);
    char ch;
    ch=n;
    puts(ch);
    
    getch();
    return 0;
}
