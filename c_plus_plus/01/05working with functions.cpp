#include <iostream>
using namespace std;

//working of function calls
//its uses stack memory
//LIFO method is applied(last in first out) like a stack of books
//when one function is called it is pushed in stack. say fun1
// when another function(fun2) is called inside fun1, then fun2 is pushed above fun1. and so on
// when too many functions are called then stack may run out of memory and stack overflow error may occur


int main() {
	// your code goes here
	return 0;
}
